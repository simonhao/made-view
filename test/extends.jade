extends(name='qq"""', age="yea,,,r_old", sex="sex?'male':'female'") layout
replace main
  div(id='qq') weibo
replace footer
  div 继承 #{title + 1}
  include(name='qq"""', age="year_old", sex="sex?'male':'female'", title="this is #{title}") module